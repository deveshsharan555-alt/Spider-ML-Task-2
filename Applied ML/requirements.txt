# --- Core (required) ---
numpy>=1.24
scikit-learn>=1.3
streamlit>=1.30

# --- Optional: Better semantic embeddings ---
# Install only if EMBEDDING = "st"
# sentence-transformers>=2.5

# --- Optional: Groq LLM for fluent answers ---
# Install only if GENERATOR = "groq"
# groq>=0.11
